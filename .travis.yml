os: linux
language: python
python: "3.6"


before_script:
 - cd
 - if [ ! -d ~/anaconda3 ] ; then wget https://repo.anaconda.com/archive/Anaconda3-2019.03-Linux-x86_64.sh -O ~/anaconda.sh ; fi ;
 - if [ ! -d ~/anaconda3 ] ; then bash ~/anaconda.sh -b -p $HOME/anaconda3 ; fi ;
 - eval "$(./anaconda3/bin/conda shell.bash hook)"
 - conda init
 - conda -y update conda
 - conda create -y -n cc python=3.6 anaconda || true
 - conda activate cc
 - conda install -y tensorflow setuptools seaborn mpi4py cloudpickle joblib tqdm
    
script:
   - conda activate cc
   - pip install -e .
   - cd gym
   - pip install -e .
   - cd ../PycoGYM/gym_pyco
   - pip install -e .
   - cd ../..
   - python spinup/run.py ppo_pyco --env warehouse_manager-v0 --num_cpu 1
   - ls -R
   

cache:
  directories:
    - '$HOME/anaconda3'


